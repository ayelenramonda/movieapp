class Prestamo {
    constructor(nombre, taza, cuotas, limite, tipodeprestamo) {
      this.nombre = nombre;
      this.taza = taza;
      this.cuotas = cuotas;
      this.limite = limite;
      this.tipodeprestamo = tipodeprestamo;
    }
}