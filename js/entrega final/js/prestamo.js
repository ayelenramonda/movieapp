class Prestamo {
    constructor(nombre, taza, cuotas, limite, tipodeprestamo, moneda) {
      this.nombre = nombre;
      this.taza = taza;
      this.cuotas = cuotas;
      this.limite = limite;
      this.limiteUSD = limiteUSD;
      this.tipodeprestamo = tipodeprestamo;
      this.moneda = moneda;
    }
}